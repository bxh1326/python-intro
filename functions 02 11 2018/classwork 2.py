# Write a Python function that takes a list of words and
# returns the length of the longest one

# myTest = ['this', 'is', 'a', 'list', 'of', 'words']
def longestWord(myTest):
    myTest = ['this', 'is', 'a', 'list', 'of', 'words']
    # noinspection PyArgumentList
    len()
